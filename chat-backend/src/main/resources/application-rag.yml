spring:
  # ═══════════════════════════════════════════════
  # Database — a ser preenchido pelo dev de banco
  # ═══════════════════════════════════════════════
  datasource:
    url: jdbc:postgresql://localhost:5432/chatdb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      initialization-fail-timeout: 30000
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

rag:
  topK: ${RAG_TOP_K:5}
  ollama:
    url: ${RAG_OLLAMA_URL:http://localhost:11434}
    model: ${OLLAMA_MODEL:gemma3:4b}
    embedding-model: ${OLLAMA_EMBEDDING_MODEL:nomic-embed-text}
    connect-timeout: ${OLLAMA_CONNECT_TIMEOUT:10s}
    read-timeout: ${OLLAMA_READ_TIMEOUT:120s}
    temperature: ${OLLAMA_TEMPERATURE:0.7}
    max-tokens: ${OLLAMA_MAX_TOKENS:4096}
  chunking:
    size: ${CHUNK_SIZE:1000}
    overlap: ${CHUNK_OVERLAP:150}
  webhook:
    url: ${N8N_WEBHOOK_URL:http://localhost:5678/webhook/ingestion-complete}
    enabled: ${N8N_WEBHOOK_ENABLED:true}
    retry-attempts: ${N8N_WEBHOOK_RETRY_ATTEMPTS:3}
    retry-delay: ${N8N_WEBHOOK_RETRY_DELAY:1000}

ai:
  agent:
    enabled: true
    base-url: ${RAG_OLLAMA_URL:http://localhost:11434}
    model: ${OLLAMA_MODEL:gemma3:4b}
    read-timeout: 120000
    connect-timeout: 10000
    temperature: ${AI_AGENT_TEMPERATURE:0.7}
    max-tokens: ${AI_AGENT_MAX_TOKENS:2048}

logging:
  level:
    com.project.chat: INFO
