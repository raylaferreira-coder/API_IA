server:
  port: 8080

spring:
  application:
    name: chat-backend
  profiles:
    active: dev
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 11MB

chat:
  storage:
    upload-dir: ./uploads
  session:
    expiration-hours: 24
